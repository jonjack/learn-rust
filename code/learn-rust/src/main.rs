/*
    Example of a separate binary in the same project.
    You can compile with `cargo build --bin bin1`
    Tou can compile+run with `cargo run --bin bin1`
*/
fn main() {
    println!("Hello, world!");
}
